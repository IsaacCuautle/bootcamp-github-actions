name: deploy
on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout 
        uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: instalar dependencias
        run: npm i  

      - name: testear
        run: npm run test

    deploy:
      
      steps:
        - name: deploy
          run: echo "success deployment"