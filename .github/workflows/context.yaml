# name: context

# on: push

# jobs: 

#   job:

#     # Condiciona el flujo de trabajo segun la rama 
#     if: ${{github.ref == 'refs/heads/main'}} 
    
#     runs-on: ubuntu-latest

#     steps:
#       - name: "Desde context.yaml"
#         run: echo "Desde context.yaml"